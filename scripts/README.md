# scripts/

Place all executable scripts in this directory instead of running commands directly in the terminal.

## Naming conventions

- Python scripts: `script_name.py`
- PowerShell scripts: `script_name.ps1`
- Shell scripts: `script_name.sh`

## Usage

Run scripts manually from the terminal, e.g.:
```
python scripts/migrate.py
```

